import { Request, Response } from "express";

// Placeholder for job matching logic
export const getMatches = (req: Request, res: Response) => {
  res.status(200).json({ message: "Job matches retrieved (placeholder)" });
};
